<% if data.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(data.errors.count, "error") %> prohibited this data from being saved:</h2>
    <ul>
    <% data.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
